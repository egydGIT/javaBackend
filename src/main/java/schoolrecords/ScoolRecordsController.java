/*
package scoolrecords;

import java.util.Random;
import java.util.Scanner;

public class ScoolRecordsController {
    ClassRecords classRecords = new ClassRecords("5.a", new Random());         // mi az a Random objektum?

    public void printMenu() {
        System.out.println(
        "1. Diákok nevének listázása\n" +
        "2. Diák név alapján keresése\n" +
        "3. Diák létrehozása\n" +
        "4. Diák név alapján törlése\n" +
        "5. Diák feleltetése\n" +
        "6. Osztályátlag kiszámolása\n" +
        "7. Tantárgyi átlag kiszámolása\n" +
        "8. Diákok átlagának megjelenítése\n" +
        "9. Diák átlagának kiírása\n" +
        "10. Diák tantárgyhoz tartozó átlagának kiírása\n" +
        "11. Kilépés"
        );
    }

    public void runMenu () {
        Scanner scanner = new Scanner(System.in);

        System.out.println("Kérem válasszon a fenti menüpontok közül! ");
        String choosenMenu = scanner.nextLine();

 */

        /*----------
        if (choosenMenu.contains("1.") {
            classRecords.listStudentNames();                            // 1.
        }
         */

/*
        if (choosenMenu.contains("2.") {
            System.out.println("Kérem adja meg a keresett diák nevét! ");
            String name = scanner.nextLine();

            classRecords.findStudentByName(name);                       // 2.
        }
        if (choosenMenu.contains("3.") {
            System.out.println("Kérem adja meg az új diák nevét! ");
            String student = scanner.nextLine();

            classRecords.addStudent(student);                           // 3.
        }
        if (choosenMenu.contains("4.") {
            classRecords.findStudentByName().removeStudent();           // 4.
        }

 */

        /* --------------

        if (choosenMenu.contains("5.") {
            classRecords.repetition();                                  // 5.
        }
        if (choosenMenu.contains("6.") {
            classRecords.calculateClassAverage();                       // 6.
        }
        if (choosenMenu.contains("7.") {
            classRecords.calculateClassAverageBySubject();              // 7.
        }
        if (choosenMenu.contains("8.") {
            classRecords.listSudyResults();                             // 8.
        }
        if (choosenMenu.contains("9.") {
            classRecords.findStudentByName().calculateAverage();        // 9.
        }
        if (choosenMenu.contains("10.") {
            classRecords.findStudentByName().calculateSubjectAverage(); // 10.
        }
        if (choosenMenu.contains("11.") {
                                                                        // 11.
        }
    }

    public void initSchool() {

    }

         */

/*
    public static void main(String[] args) {
        ScoolRecordsController scoolRecordsController = new ScoolRecordsController();





    }
}



 */